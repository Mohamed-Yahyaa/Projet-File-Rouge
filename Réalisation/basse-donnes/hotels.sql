CREATE TABLE `hotels` (
    id int PRIMARY KEY AUTO AUTO_INCREMENT(11) ,
  Nom_Hotel varchar(255) NOT NULL,
 `Description`  varchar(255) NOT NULL,
  Photo varchar(255) NOT NULL,
   Adress varchar(255) NOT NULL );
 	